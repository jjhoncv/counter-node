Offer Reminder Application
========================

Project to send emails of offers with countdown of completion time.

Requirements
------------

  * Node 9.5.0;

Installation
------------

Install the project and run this command:

```bash
$ npm install
```

Usage
-----

There's no need to configure anything to run the application. <http://localhost:3000>:

```bash
$ npm run start
```
